
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Production-ready rules: Only authenticated users can read/write
      allow read, write: if request.auth != null;
    }
  }
}
